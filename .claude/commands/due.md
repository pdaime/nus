---
description: What's due. Tonight by default, or a given day/range (e.g. /due friday, /due this week).
---

Answer what's due for: $ARGUMENTS (if empty, default to **tonight/today**).

1. Check today's date first.
2. Read `school/semester-overview.md` (combined calendar; open per-class
   files only if detail is needed).
3. Answer in a tight table: class | what's due. Then a short "on deck" list
   for the next 3–4 days; bold any exam within 7 days.
4. If something relevant is overdue, say so plainly.
5. If a crunch week starts within 7 days, add a one-line warning.
6. If `/setup` hasn't been run (no overview), say so and point them to it.
   Never guess deadlines.
